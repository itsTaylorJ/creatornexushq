.firebase/
node_modules/
.DS_Store

# UI/UX Pro Max skills (installed per-dev via `uipro init`)
.claude/skills/

# Agent tooling artifacts — not project code
.agents/
.playwright-mcp/
# AGENTS.md is a generated mirror of CLAUDE.md. Two copies of project context
# is exactly how one goes stale and causes a wrong call — CLAUDE.md is the one.
AGENTS.md
